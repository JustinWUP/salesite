<%= form_for(@openstate) do |f| %>
  <% if @openstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :onstate, "Turn off store?" %><br />
    <%= f.check_box :onstate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>